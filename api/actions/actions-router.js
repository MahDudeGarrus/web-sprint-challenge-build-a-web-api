const express = require("express")
const router = express.Router();
const Action = require('./actions-model.js')

// ACTION ENDPOINTS

router.get('/api/actions', (res, req) => {

})

router.get('/api/actions/:id', (res, req) => {

})

router.post('/api/actions', (res, req) => {

})

router.put('/api/actions/:id', (res, req) => {

})

router.delete('/api/actions/:id', (res, req) => {

})