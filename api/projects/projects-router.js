const express = require("express")
const router = express.Router();
const Project= require('./projects-model.js');

// PROJECT ENDPOINTS

router.get('/api/projects', (res, req) => {

})

router.get('/api/projects/:id', (res, req) => {

})

router.post('/api/projects', (res, req) => {

})

router.put('/api/projects/:id', (res, req) => {

})

router.delete('/api/projects/:id', (res, req) => {

})

router.get('/api/projects/:id/actions', (res, req) => {

})